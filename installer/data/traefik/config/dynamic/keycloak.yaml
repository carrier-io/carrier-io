http:
  routers:
    keycloak:
      rule: "PathPrefix(`/auth`)"
      entryPoints:
      - "http"
      service: "keycloak"
  services:
    keycloak:
      loadBalancer:
        servers:
        - url: "http://keycloak:8080/"
